/**
 * Kept as the stable import path for tour lookups. The content itself lives in
 * `./content`, where each domain section doubles as source for the written
 * guide.
 */
export { GUIDE_SECTIONS, TOURS, getGuideSection, getTour } from './content';
